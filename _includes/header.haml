%header.site-header{ role: "banner"}
  .wrapper
    {% assign default_paths = site.pages | map: "path" %}
    {% assign page_paths = site.header_pages | default: default_paths %}
    %a.site-title{ href: "{{ "/" | relative_url }}">{{ site.title | escape }}}
